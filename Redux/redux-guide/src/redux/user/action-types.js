// É aqui que guardamos os types dentro de variáveis para armazenar o dado de forma segura;

const UserActionTypes = {
    // É recomendado colocar esse modelo de caminho entidade/evento 
    LOGIN: "user/login",
    LOGOUT:"user/logout"
}

export default UserActionTypes;